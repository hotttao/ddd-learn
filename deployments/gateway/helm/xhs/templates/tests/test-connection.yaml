apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xhs.fullname" . }}-test-connection"
  labels:
    {{- include "xhs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xhs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
