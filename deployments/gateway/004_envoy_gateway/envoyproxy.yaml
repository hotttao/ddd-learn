apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyProxy
metadata:
  name: public-proxy
  namespace: ddd-learn
spec:
  provider:
    type: Kubernetes
    kubernetes:
      envoyService:
        type: NodePort
