apiVersion: apps/v1
kind: Deployment
metadata:
  name: mailpit
  namespace: ddd-learn
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: mailpit
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mailpit
    spec:
      containers:
        - name: mailpit
          image: axllent/mailpit:v1.27.8
          args:
            # 让页面、静态资源和 API 都位于 Envoy 的 /mailpit 前缀下。
            - --webroot
            - /mailpit/
          ports:
            - name: smtp
              containerPort: 1025
            - name: http
              containerPort: 8025
          readinessProbe:
            httpGet:
              path: /mailpit/
              port: http
          livenessProbe:
            httpGet:
              path: /mailpit/
              port: http
---
apiVersion: v1
kind: Service
metadata:
  name: mailpit
  namespace: ddd-learn
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mailpit
  ports:
    - name: smtp
      port: 1025
      targetPort: smtp
    - name: http
      port: 8025
      targetPort: http
