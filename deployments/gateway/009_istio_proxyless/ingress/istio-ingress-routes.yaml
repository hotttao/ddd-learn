# Istio Gateway API Controller 管理的入口路由。
# parentRefs 指向 gatewayClassName=istio 的 istio-ingress，不依赖 Envoy Gateway。
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: istio-ui-example
  namespace: ddd-learn-sidecar
spec:
  parentRefs:
    - name: istio-ingress
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ui-example
          port: 80
---
# Kratos 的服务端点本身从根路径开始，因此入口需要移除 /kratos 前缀。
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: istio-kratos-public
  namespace: ddd-learn-sidecar
spec:
  parentRefs:
    - name: istio-ingress
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /kratos
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: kratos-public
          port: 80
---
# Mailpit 已配置 base path=/mailpit，所以这里保留请求前缀。
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: istio-mailpit
  namespace: ddd-learn-sidecar
spec:
  parentRefs:
    - name: istio-ingress
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /mailpit
      backendRefs:
        - name: mailpit
          port: 8025
---
# XHS HTTP/JSON 请求保留 /v1/xhs 前缀；Transcoder 会根据 descriptor 将请求转换为 gRPC。
# 后端切换到独立的 xhs_grpc，旧的 xhs_service HTTP 服务不参与本实验链路。
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: istio-xhs-service
  namespace: ddd-learn-sidecar
spec:
  parentRefs:
    - name: istio-ingress
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /v1/xhs
      backendRefs:
        - name: xhs-grpc-service
          port: 80
