# Traefik static configuration for the local authentication gateway.
entryPoints:
  web:
    address: ":80"

providers:
  file:
    # Dynamic HTTP routers and services are kept separately for teaching.
    filename: /etc/traefik/dynamic.yml
    watch: true

log:
  level: INFO

accessLog: {}
